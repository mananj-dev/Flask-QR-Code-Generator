# Flask QR Code Generator

This is a simple web application built with Flask that generates QR codes from user input text.

## Technologies Used

- Python
- Flask
- HTML/CSS
- qrcode library

## How to Run

1. Clone the repository
2. Install dependencies

pip install -r requirements.txt

3. Run the app

python app.py
